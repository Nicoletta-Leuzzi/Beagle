<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Permessi minimi -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Beagle">
        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="false" />
        <!-- Registrazione (nessun intent-filter) -->
        <activity
            android:name=".ui.welcome.RegisterActivity"
            android:exported="false" /> <!-- Login: schermata iniziale -->
        <activity
            android:name=".ui.welcome.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Main (schermata interna) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>