<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Shown as an action if there is room -->
    <item
        android:id="@+id/action_pet_profile"
        android:contentDescription="@string/pet_profile_description"
        android:icon="@drawable/ic_account_circle"
        app:showAsAction="ifRoom|withText" />

    <item
        android:id="@+id/action_new_chat"
        android:contentDescription="@string/new_chat_description"
        android:icon="@drawable/add_icon"
        app:showAsAction="ifRoom|withText" />
</menu>